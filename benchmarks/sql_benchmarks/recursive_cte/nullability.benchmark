name nullable_self_ref_filter
group recursive_cte

init
SET datafusion.execution.enable_recursive_ctes = true;

run
WITH RECURSIVE t(n) AS (
  SELECT 1 AS n
  UNION ALL
  SELECT n + 1 FROM t
  WHERE n IS NOT NULL AND n < 10000
)
SELECT count(*) FROM t;
